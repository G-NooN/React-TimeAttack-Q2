import styled from "styled-components";

// Section Title
const StSectionTitle = styled.h2`
  margin-left: 5px;
`;

// Item Info Title
const StTitle = styled.span`
  font-weight: bold;
  margin-left: 5px;
`;

const StButton = styled.button`
  margin: 0 3px;
`;

export { StSectionTitle, StTitle, StButton };
